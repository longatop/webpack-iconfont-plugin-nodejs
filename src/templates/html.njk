<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>Icon Fonts Preview</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ htmlCssFile }}">
    <style>
        body {
            font-family: arial,sans-serif;
        }
        .container {
            max-width: 960px;
            margin: 0 auto;
            padding: 20px;
        }
        h3 {
            font-size: 24px;
            color: #333;
            padding: 12px 0 20px;
            margin: 40px 0;
            border-bottom: solid 1px #eee;
            position: relative;
            font-weight: normal;
        }
        h3 small {
            font-size: 16px;
            color: #666;
        }
        h3::before {
            content: "";
            display: block;
            width: 60px;
            height: 4px;
            background: #e31937;
            position: absolute;
            top: 0;
            left: 0;
        }
        p {
            margin: 0 0 20px;
            font-size: 16px;
            line-height: 1.4;
            color: #666;
        }
        .flex-tb {
            display: flex;
            flex-flow: wrap;
            margin-bottom: 30px;
        }
        .flex-tb > section {
            display: flex;
            flex-flow: column;
            flex: 0 0 192px;
            align-items: center;
            padding-bottom: 30px;
        }
        .flex-tb > section > header {
            height: 30px;
            outline: none;
            color: #666;
        }
        .flex-tb i {
            font-size: 40px;
            line-height: 1;
            display: block;
            color: #333;
        }
        .flex-tb div:hover {
            position: absolute;
            background: #fff;
            padding: 15px;
            border: 1px solid #eee;
            top: 20px;
            z-index: 99;
        }
        .flex-tb div:hover > i {
            font-size: 100px;
            color: #e31937;
        }
        input { border: 0; outline: 0; width: 150px; text-align: center; background: transparent; }
        section { position: relative; }
    </style>
    <script>
        function copyCls(el){
            el = el.querySelector('input')
            el.focus();
            el.select()
            document.execCommand('copy', false, null);            
        }
    </script>
<body>
    <div class="container">
        <h3>Iconfont preview<small> ({{ glyphs.length }} icons, click to copy)</small></h3>
        <div class="flex-tb">
        {% for glyph in glyphs %}
            <section onclick="copyCls(this)">
                <input value="{{ cssPrefix }}-{{ glyph.name }}" />
                <div><i class="{{ cssPrefix }}-{{ glyph.name }}"></i></div>
            </section>
        {% endfor %}
        </div>
        <h3>How to use</h3>
        <p>Modify or add a svg file in dir [src/assets/svgs], they will be parsed to iconfonts with css, and hot-loaded.</p>
        <p>Class name prefix: <b>{{ cssPrefix }}-</b> &emsp; You can use it like: &lt;i class="{{ cssPrefix }}-xxx"&gt;&lt;/i&gt;</p>
    </div>
</body>
</html>
