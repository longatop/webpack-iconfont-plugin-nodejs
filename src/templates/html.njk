<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>IconFonts Preview Demo -- {{ fontName }}</title>
    <meta name="description" content="An Icon Font Generated By webpack-iconfont-plugin-nodejs">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ htmlCssFile }}">
    <style>
        .info {
            padding: 0 20px;
        }

        .flex-tb {
            display: flex;
            flex-flow: wrap;
            margin-bottom: 150px;
        }

        .flex-tb > section {
            display: flex;
            flex-flow: column;
            flex: 0 0 240px;
            align-items: center;
            padding-bottom: 50px;
        }

        .flex-tb > section > header {
            height: 30px;
            outline: none;
        }

        .flex-tb i {
            font-size: 40px;
            line-height: 1;
            display: block;
        }

        .flex-tb div:hover {
            position: absolute;
            background: #fff;
            border: 1px solid #ddd;
            top: 20px;
        }

        .flex-tb div:hover > i {
            font-size: 200px;
        }
        body{
            background: #ddd;
        }
        td { padding: 5px 10px; }
        input { border: 0; outline: 0; width: 150px; text-align: center; background: transparent; }
        section { position: relative; }
    </style>
    <script>
        function copyCls(el){
            el = el.querySelector('input')
            el.focus();
            el.select()
            document.execCommand('copy', false, null);            
        }
    </script>
<body>
    <h1>Iconfont preview</h1>
    <h4>
    Modify or add a svg file in dir [src/assets/svgs], they will be parsed to iconfonts with css, and hot-loaded.</h4>
    <hr/>
    <div class="info">
        <h3>{{ fontName }} ({{ glyphs.length }} icons, click to copy)</h3>
        Class name prefix: <b>{{ cssPrefix }}-</b> &emsp; You can use it like: &lt;i class="my-icon-success"&gt;&lt;/i&gt;
    </div>
    <hr/>
    <div class="flex-tb">
      {% for glyph in glyphs %}
        <section onclick="copyCls(this)">
            <input value="{{ cssPrefix }}-{{ glyph.name }}" />
            <div><i class="{{ cssPrefix }}-{{ glyph.name }}"></i></div>
        </section>
      {% endfor %}
    </div>
</body>
</html>
